function strongclassifier = init_strongclassifier(I, patch)
    global parameter;
    % haarfeature is global variable
    % in function generaterandomfeature the generateed haar feature is
    % stored in haarfeature
    % then init the gaussian distribution
    strongclassifier = struct('area', [] , ...
                              'type', [] , ...
                              'location', [] , ...
                              'weight', [] , ...
                              'index', [] , ...
                              'posgaussian', [] , ...
                              'neggaussian', [] , ...
                              'correct', [] , ...
                              'wrong', [] , ...
                              'weightvalue', []);
    for i = 1:parameter.numselectors
        if parameter.edgefeature
            strongclassifier(i) = generaterandomfeature_edge(I, parameter.numweakclassifiers , patch, parameter.minarea);
        else
            strongclassifier(i) = generaterandomfeature(parameter.numweakclassifiers , patch, parameter.minarea);
        end
    end
   % gaussiandistributioninit( [haarfeature.mean, haarfeature.sigma]);
end